[cherryPlan]
	portedRegex = ^\\(cherry picked from commit (.*)\\)$
	portedRegex = ^\\(backported from commit (.*)\\)$
[changelog]
	sections = highlight,regression,cli,transaction,security,spec,fileformat,generator,api,build,other
	ignore = cleanup,test,documentation
[changelog "headings"]
	api = API
	build = Build system
	cli = Command line
	security = Security
	fileformat = File format
	generator = Generators
	highlight = Highlights
	other = Other
	regression = Regression fixes
	spec = SPEC format
	transaction = Transactions
[changelog "aliases"]
	crypto = security
	lua = api
	python = api
	rfe = highlight
