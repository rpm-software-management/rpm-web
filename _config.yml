defaults:
  - scope:
      path: ""
      type: "releases"
    values:
      project: rpm
      layout: release
      heading_offset: 0
      overview: true
      snapshot: false
      draft: false
  - scope:
      path: "docs"
    values:
      layout: default

collections:
  releases:
    output: true

heading_level: 4
exclude: ["_build"]
